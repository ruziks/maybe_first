#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;


void replace(int &t) {
	if (t % 2 != 0) {
		t = 0;
	}
	else {
		t = t / 2;
	}
	
}
int main() {
	int x;
	cin >> x;
	replace(x);
	cout<<x;
}
