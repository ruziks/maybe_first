#include <iostream>
#include <fstream>
#include <forward_list>

using namespace std;

int main() {
    ifstream input("input.txt");
    ofstream output("output.txt");
    forward_list<int> flist;
    int x, y, n;
    input >> n;
    for (int i = 0; i < n; i++) {
        int num;
        input >> num;
        flist.push_front(num);
    }
    while (!file.eof()) {
        int num;
        input >> num;
        s.push(num);
    }
    input >> x >> y;
    for (auto it = flist.begin(); it != flist.end(); it++) {
        output << *it << " ";
        if (*it == x) {
            output << y << " ";
        }
    }
    input.close();
    output.close();
    return 0;
}
