#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
	float s = 0;
	int n; float x;
	cin >> n >> x;
	for (int i = 1; i <= n; i++) {
		s += sin(x);
		x = x * x;
	}
	cout << s;
}
