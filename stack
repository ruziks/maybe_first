#include <iostream>
#include <fstream>
#include <stack>

using namespace std;

int main() {
    ifstream input("input.txt");
    ofstream output("output.txt");
    stack<int> s;
    int x, y;
    while (!file.eof()) {
        int num;
        input >> num;
        s.push(num);
    }
    input >> x >> y;
    while (!s.empty()) {
        int num = s.top();
        s.pop();
        output << num << " ";
        if (num == x) {
            output << y << " ";
        }
    }
    input.close();
    output.close();
    return 0;
}
