#include<iostream>
using namespace std;
int main(){
    char str[20];
    int k=0;
    cin.getline(str,20);
    for (int i=0; i<sizeof(str); i++){
        if (str[i] == ' '){
            k++;
        } 
    }
    k++;
    cout<<k<<endl;
    return 0;
}
