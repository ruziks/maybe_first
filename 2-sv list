#include <iostream>
#include <fstream>
#include <list>

using namespace std;

int main() {
    ifstream input("input.txt");
    ofstream output("output.txt");
    list<int> lst;
    int x, y, n;
    input >> n;
    for (int i = 0; i < n; i++) {
        int num;
        input >> num;
        lst.push_back(num);
    }
    input >> x >> y;
    for (auto it = lst.begin(); it != lst.end(); it++) {
        output << *it << " ";
        if (*it == x) {
            lst.insert(it++, y);
        }
    }
    for (auto it = lst.begin(); it != lst.end(); it++) {
        output << *it << " ";
    }
    input.close();
    output.close();
    return 0;
}
