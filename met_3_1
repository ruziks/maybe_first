#include <iostream>
#include <math.h>
#include <iomanip>
using namespace std;


int main(){
	int k = 0;
	for (int i = 10; i <= 100; i++) {
		if (abs((i % 10) - (i / 10)) <= 1) {
			k += 1;
		}
	}
	cout << k;
}
